import React from 'react'
import ReactDOM from 'react-dom/client'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <div>
      <h1>🚀 JAC Smart Learning Platform</h1>
      <p>React application is running!</p>
    </div>
  </React.StrictMode>,
)
